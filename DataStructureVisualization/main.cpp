//uses conan as the package manager
//uses cmake as the build system or generator
//still need to resolve conan problems
#include <iostream>
using namespace std;

struct Node{
    int data;
    Node* link;
};

int main() {

    return 0;
}
